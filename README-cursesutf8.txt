This is the CURSES patch to Nethack 3.4.3 vanilla with fixes to work with UTF8.

Required: ncursesw-dev. Some systems have this package as a different option.

* UTF8 patches minimally pulled from nethack.alt.org.
* Updated menu selection to correctly allow toggling between graphics modes.

